# 联盟样式一站式工作台 · 部署与分享指南

> 让其他人拿到链接就能直接访问的几种方式

---

## 推荐方案：腾讯内部静态托管

### 方案A：with.woa.com（最推荐 ⭐）

腾讯内部最常用的静态资源托管，操作最简单。

**步骤**：

1. 把 `样式工作台Demo` 整个文件夹打包成 ZIP（已为你打包好：`样式工作台Demo.zip`）
2. 访问 https://with.woa.com/
3. 用RTX登录
4. 创建一个项目（例如名称：formhub-demo）
5. 上传 ZIP 解压到根目录，**确保 index.html 在根目录**
6. 部署后获得链接，类似 `https://with.woa.com/formhub-demo/`
7. 把链接发给同事，他们用RTX登录后直接访问

**优点**：腾讯内网HTTPS、内部账号自动鉴权、操作简单
**缺点**：访问需要RTX账号

---

### 方案B：iWiki/KM 附件分享

适合跟产品文档一起放，方便归档。

**步骤**：

1. 在 iWiki/KM 上新建一个产品页面
2. 写一段产品介绍 + 把 `样式工作台Demo.zip` 作为附件上传
3. 同事下载后双击 index.html 在本地打开

**优点**：和文档归档在一起，方便项目管理
**缺点**：需要下载到本地，无法在线打开

---

### 方案C：微盘 + 短链

适合临时分享、快速反馈场景。

**步骤**：

1. 把 `样式工作台Demo.zip` 上传到微盘
2. 设置共享权限（团队成员可见）
3. 复制链接发给同事

**优点**：分享最快
**缺点**：依然需要本地打开

---

### 方案D：git.woa.com Pages（适合长期演示）

适合需要版本管理、持续迭代的场景。

**步骤**：

1. 在 git.woa.com 创建私有仓库 `formhub-demo`
2. 把 `样式工作台Demo` 文件夹 push 到仓库
3. 在仓库设置中开启 Pages 服务
4. 获得形如 `https://your-name.pages.woa.com/formhub-demo/` 的链接

**优点**：版本管理、持续更新、永久访问
**缺点**：需要懂基本的git操作

---

## 操作建议

**最快速**：用方案A（with.woa.com），10分钟内即可完成

**最稳定**：用方案D（git Pages），适合作为长期产品方案展示

**最易用**：用方案B（iWiki附件），同事下载后离线打开

---

## ZIP 包内容

```
样式工作台Demo.zip
├── index.html                   首页
├── demo0-交互模式.html           AI对话→工作流的完整流转
├── demo1-洞察归因.html           组件归因+提效建议
├── demo2-规则配置.html           引导式配置+冲突校验
├── demo3-全景总览.html           流量/预算/自定义视角
└── styles.css                   共享样式
```

**总大小**：约 60KB，纯静态文件，无外部依赖
**兼容性**：Chrome/Edge/Safari 任意现代浏览器

---

## 常见问题

**Q：with.woa.com 上传后打不开？**
A：检查 ZIP 解压后 index.html 是否在根目录，不要有多层嵌套文件夹

**Q：能不能不登录就访问？**
A：腾讯内部托管平台都需要鉴权。如需公网访问，可考虑：
- 阿里云 OSS + 自定义域名（成本约¥5/月）
- Vercel / Netlify（免费，但需要绑定 GitHub）
- 腾讯云 COS + CDN（成本低）

**Q：能不能让 demo 不只是静态展示，而是真的能交互？**
A：当前demo已经包含基础交互（视角切换、tab切换、折叠展开）。如需更复杂的交互（如真实数据连接），需要研发介入做后端联调
